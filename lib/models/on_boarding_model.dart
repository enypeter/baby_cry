class OnBoardingModel {
  OnBoardingModel(
      {required this.title,
      required this.description,
      required this.centerImage,
      required this.focusImage,
      required this.focusColor});

  final String title, description, centerImage, focusImage;
      int focusColor;
}
